import { RepetirContrassenyaDirective } from './repetir-contrassenya.directive';

describe('RepetirContrassenyaDirective', () => {
  it('should create an instance', () => {
    const directive = new RepetirContrassenyaDirective();
    expect(directive).toBeTruthy();
  });
});
